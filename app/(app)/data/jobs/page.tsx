import { JobDashboard } from '@/components/job-dashboard';

export const dynamic = 'force-dynamic';

/** Job dashboard (§57) and schedules (§58). */
export default function JobsPage() {
  return <JobDashboard />;
}
